{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.email,{attr:{'placeholder':'Email'}}) }}
    {#{{ form_row(form.password,{attr:{'placeholder':'password'}}) }}#}
    {{ form_row(form.first_name,{attr:{'placeholder':'Firstname'}}) }}
    {{ form_row(form.last_name,{attr:{'placeholder':'lastname'}}) }}
    {{ form_row(form.salutation,{attr:{'placeholder':'Salutation'}}) }}
    {{ form_row(form.phone,{attr:{'placeholder':'Phone'}}) }}
    {{ form_row(form.roles) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
